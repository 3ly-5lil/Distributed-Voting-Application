apiVersion: v1
kind: Service

metadata:
  name: result
  labels:
    app: result
    tier: frontend

spec:
  type: LoadBalancer

  selector:
    app: result
    
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
