apiVersion: v1
kind: Service

metadata:
  name: vote
  labels:
    app: vote
    tier: frontend

spec:
  type: LoadBalancer

  selector:
    app: vote

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
